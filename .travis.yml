sudo: required
services:
  - docker
before_install:
  - docker build -t pciaco/pietrowebapp:latest .
script:
  - docker images tag pciaco/pietrowebapp  
after_success: 
  - docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD";
  - docker push pciaco/pietrowebapp;